# Pre-registration — P4.2 · ORB + VWAP Confirmation (two-factor confluence), PANEL scope

> **Status: DRAFT — committed before any run, HELD for operator sign-off.** Phase-4 blind batch (2026-07-10). Minimal AND-confluence; blind params; strict decisive bar. This commit **predates the panel run**.
> **Exploration-grade** (frozen-49, large-cap panel): provisional + large-cap stamp; **not** in the §4.3 scorecard.
> **Scored under PANEL scope** (`run_panel_study`).

## 0. Blueprint combination

Blueprint P4.2 (RESEARCH_FINDINGS §4.2): *take the opening-range break only if price is on the confirming side of VWAP.* A minimal **AND** of the opening-range breakout (P3.11) and the VWAP side (P3.1), **fewest knobs**. Not a Phase-3-pattern substitution.

## 1. Hypothesis (falsifiable)

On the frozen large-cap **panel** at 5-min bars, an opening-range break confirmed by the VWAP side produces a **positive net-of-cost edge** clearing the **two-part panel verdict**. **Falsified if** KILL/INSUFFICIENT.

## 2. Economic rationale (mechanism, not pattern) — PURELY a-priori

The VWAP side filters **low-quality opening breaks**: a break of the initial-balance range (P3.11) that is also on the confirming side of the day's volume-weighted consensus is likelier the real directional resolution than a break that immediately fights VWAP. Purely a-priori; honest caution — it inherits ORB's crowding/decay and trades less than raw ORB.

## 3. Spec (event → entry → exit/holding → position)

In [`src/lab/research/strategies/orb_vwap.py`](../../src/lab/research/strategies/orb_vwap.py). `OrbVwapSpec`, symmetric, sparse: after the opening-range window closes, LONG when `close > OR_high·(1+buffer)` AND `close > VWAP`; SHORT the mirror. Ride to square-off.

**Causal / no-lookahead (hard precondition).** `opening_range` (running then fixed) + VWAP (intraday-cumulative) are point-in-time; entries only after the window closes; a signal at bar `i` uses only bars `≤ i`. **Prefix-invariance test** (`test_orb_vwap_spec.py::test_orb_vwap_prefix_invariance`) = hard precondition, **PASSED**.

## 4. Pre-committed parameters (FROZEN before the run, set BLIND)

| Parameter | Blind value | Sweep |
|---|---|---|
| `opening_range_minutes` | **30** | ±15 |
| `break_buffer` | **0.001** | ±0.0005 |

**5 configs.** **Chosen BLIND, a-priori** (classic 30-min ORB window, 0.1% buffer). **Guardrail:** nothing from the gradient / sparse-big-move / `POST_PROJECT_DIRECTIONS.md`. Registry pinned by `test_registry.py`.

## 5. Verdict — two-part PANEL verdict (unchanged) + STRICT decisive bar

Standing thresholds, **unchanged.** Less-negative is a KILL. **§6 is informational only — the full battery always runs.**

## 6. Cost-viability pre-check (INFORMATIONAL only — no battery skipped)

**Landing (informational): viable-side** — RELIANCE base config → **2,488 trades** (0.88/day ≈ one break/day), median `|gross/trade|` **0.524% > 0.182%**. Color only; the full battery runs.

## 7. Method-variants (ledger-charged, NOT owed)

- 15-min window; a volume-filtered ORB; a VWAP-band (not just side). None run now.

## 8. Universe & data (PANEL scope)

- **Panel (scored):** frozen 10 (Lock A) — HDFCBANK, RELIANCE, ICICIBANK, TCS, TATASTEEL, ULTRACEMCO, TITAN, COALINDIA, DRREDDY, TATACONSUM. **6d held-out:** INFY, SBIN, SUNPHARMA, ADANIPORTS, NESTLEIND.
- **Interval / range:** 5-minute; 2015-02-02 → 2026-07-03, regular-session-filtered.
- **Scope stamp (auto):** large-caps only; a KILL means "no edge on large-caps".

## 9. Commitment

- Params, spec, thresholds **frozen before the run**, blind, a-priori. No-lookahead precondition PASSED. Panel verdict believed as-is; no knobs to rescue. If it passes → STOP + flag + verdict-grade path. Gradient / sparse-big-move / `POST_PROJECT_DIRECTIONS.md` did NOT shape this prereg.

## 10. Run commands (post-sign-off only)

```bash
uv run python scripts/run_panel_study.py --strategy orb_vwap \
    --interval 5minute --start 2015-02-02 --end 2026-07-03 \
    --data-root data --ledger-dir data/ledger --config-dir config
```

Panel + held-out from the frozen `study_panel.yaml`. Exploration-grade; not written to the scorecard.
