# Pre-registration — P4.1 · VWAP + Breakout + Volume Surge (three-way confluence), PANEL scope

> **Status: DRAFT — committed before any run, HELD for operator sign-off.** Phase-4 blind batch (operator ruling 2026-07-10). Minimal AND-confluence; blind params; strict decisive bar. This commit **predates the panel run**.
> **Exploration-grade** (frozen-49 survivor-only, large-cap panel): provisional + large-cap scope stamp; **not** in the `RESEARCH_FINDINGS.md §4.3` scorecard.
> **Scored under PANEL scope** (`run_panel_study`, per-symbol-then-aggregate).

## 0. Blueprint combination (not a Phase-3-pattern substitution)

The blueprint's P4.1 (RESEARCH_FINDINGS §4.2): *take a range break only when it happens on the trend side of VWAP AND on a relative-volume surge — three-way confluence.* A minimal **AND** of three Phase-3 primitives (range break P3.2, VWAP side P3.1, volume surge P3.2), **fewest knobs**. Not motivated by any Phase-3 least-bad pattern.

## 1. Hypothesis (falsifiable)

On the frozen large-cap **panel** at 5-min bars, a range break confirmed by both the VWAP side and a volume surge produces a **positive net-of-cost edge** clearing the **two-part panel verdict** (aggregate seven-point gate AND breadth). **Falsified if** the panel verdict is KILL/INSUFFICIENT.

## 2. Economic rationale (mechanism, not pattern) — PURELY a-priori

Three independent confirmations should cut false breaks: a break **through the prior range** (P3.2) that is **on the trend side of VWAP** (aligned with the day's volume-weighted consensus, P3.1) and **on a participation surge** (real flow, not a thin drift) is likelier to be a genuine resolution than any leg alone. Purely a-priori; honest caution — each added condition also inflates the search space (DSR deflation matters most here), and confluence cuts the sample.

## 3. Spec (event → entry → exit/holding → position)

In [`src/lab/research/strategies/vwap_breakout_volume.py`](../../src/lab/research/strategies/vwap_breakout_volume.py). `VwapBreakoutVolumeSpec`, symmetric, sparse: LONG when `relative_volume ≥ vol_mult` AND `close > intraday_donchian_high` AND `close > VWAP`; SHORT the mirror. Ride to the MIS square-off (adapter forward-fills, flat next open).

**Causal / no-lookahead (hard precondition).** VWAP (intraday-cumulative), `intraday_donchian` (excludes current bar), `relative_volume` (trailing) — all point-in-time; a signal at bar `i` uses only bars `≤ i`, fills at `i+1`'s open. **Prefix-invariance test** (`test_vwap_breakout_volume_spec.py::test_vwap_breakout_volume_prefix_invariance`) = hard precondition, **PASSED**.

## 4. Pre-committed parameters (FROZEN before the run, set BLIND)

| Parameter | Blind value | Sweep |
|---|---|---|
| `breakout_lookback` | **20** | ±5 |
| `vol_mult` | **1.5** | ±0.25 |
| `volume_period` | **20** | fixed |

**5 configs.** **Chosen BLIND, a-priori** (textbook: 20-bar range, 1.5× volume surge). **Guardrail:** nothing drawn from the turnover-gradient / sparse-big-move observations / `POST_PROJECT_DIRECTIONS.md`. Minimal 2-knob surface. Registry pinned by `tests/unit/test_registry.py`.

## 5. Verdict — two-part PANEL verdict (unchanged) + STRICT decisive bar

Judged by `run_panel_study` at the pre-committed `config/killgate.yaml` thresholds, **unchanged.** **STRICT decisive bar:** less-negative is a KILL. **§6 is informational only (Phase-4 ruling) — the full battery always runs; §6 never gates.**

## 6. Cost-viability pre-check (INFORMATIONAL only — no battery skipped)

Median `|Trade.gross_return|` on RELIANCE vs ~**0.182%** cost, reported as color. **Landing (informational): viable-side** — RELIANCE base config → **4,068 trades** (1.44/day), median `|gross/trade|` **0.432% > 0.182%**. Color only; the full battery runs regardless.

## 7. Method-variants (ledger-charged, NOT owed)

- 15-min frequency; a global (multi-session) range instead of intraday; a different volume window. None run now.

## 8. Universe & data (PANEL scope)

- **Panel (scored):** frozen 10 (Lock A) — HDFCBANK, RELIANCE, ICICIBANK, TCS, TATASTEEL, ULTRACEMCO, TITAN, COALINDIA, DRREDDY, TATACONSUM. **6d held-out:** INFY, SBIN, SUNPHARMA, ADANIPORTS, NESTLEIND.
- **Interval / range:** 5-minute; 2015-02-02 → 2026-07-03, regular-session-filtered.
- **Scope stamp (auto):** large-caps only; a KILL means "no edge on large-caps".

## 9. Commitment

- Params, spec, thresholds **frozen before the run**, blind, a-priori; this commit is the proof. No-lookahead precondition PASSED. Panel verdict believed as-is; no tuning / no knobs to rescue. If it passes → STOP + flag + verdict-grade path. Gradient / sparse-big-move / `POST_PROJECT_DIRECTIONS.md` did NOT shape this prereg.

## 10. Run commands (post-sign-off only)

```bash
uv run python scripts/run_panel_study.py --strategy vwap_breakout_volume \
    --interval 5minute --start 2015-02-02 --end 2026-07-03 \
    --data-root data --ledger-dir data/ledger --config-dir config
```

Panel + held-out from the frozen `study_panel.yaml`. Exploration-grade; not written to the scorecard.

---

## Result (2026-07-10) — KILL (full panel battery) — exploration-grade

> **Exploration-grade** (frozen-49, large-cap). §0–§10 are the blind PLAN; this block is the OUTCOME. Program ledger **71 → 76** (5 configs). Not in the §4.3 scorecard.

**Cost-viability (§6, informational — Ruling 2):** viable-side (RELIANCE 4,068 trades, median 0.432% > cost). The full battery ran regardless.

**Panel verdict: KILL** — Part 1 KILL, Part 2 breadth FAIL. Breadth median per-symbol CPCV path-Sharpe **−3.580**, positive fraction **0.00**; aggregate CPCV median **−8.202**, DSR 0.000, PBO 0.000, effective trials 8.90.

| # | Criterion | Result |
|---|---|---|
| 1 | CPCV median path-Sharpe > 1.0 | **FAIL** — −8.202 |
| 2 | DSR ≥ 0.95 | **FAIL** — 0.000 |
| 3 | PBO < 0.20 | **PASS** — 0.000 |
| 4 | CPCV ≥ 90% positive & p10 ≥ 0 | **FAIL** — positive 0.00, p10 −9.607 |
| 5 | PF ≥ 1.3, top-5 < 40%, net expectancy > 0 | **FAIL** — PF 0.25, top5 0.07, net expectancy −0.00238 |
| 6 | Robustness battery | **FAIL** — param −8.28, MC 0.00, cross-sym 0.00, noise False; two-engine reconciles ✓ |
| 7 | Regime stability | **FAIL** — positive in no bucket |

**Diagnosis:** the three-way confluence did **not** manufacture an edge — aggregate −8.202, PF 0.25, 0/10 positive, ≈ the single-factor breakout (P3.2 −8.753). Requiring the VWAP side + a volume surge on top of the range break neither helped nor cleared the strict bar. KILL.

**Disposition.** Honest logged negative; blind base, nothing tuned. Exploration-grade / large-cap; not in the §4.3 scorecard.
