# Pre-registration — P3.11 · Opening Range Breakout (ORB), PANEL scope

> **Status: DRAFT — committed before any run, HELD for operator sign-off.** Part of the P3.10–P3.14 blind batch (operator ruling 2026-07-10). Single continuation study; blind params; strict decisive bar. This commit **predates the panel run**.
> **Exploration-grade** (frozen-49 survivor-only, large-cap panel): provisional / upper-bound + large-cap scope stamp regardless of PASS/KILL; **not** in the `RESEARCH_FINDINGS.md §4.3` scorecard.
> **Scored under PANEL scope** (`run_panel_study`).

## 0. A single self-contained study

ORB is a single **breakout/continuation** mechanism (break of the opening range). Not a directional dichotomy (a symmetric long/short break). Method choices (window length, buffer, a volume filter) are non-owed variants (§7).

## 1. Hypothesis (falsifiable)

On the frozen large-cap **panel** at 5-min bars, trading the break of the first-N-minute opening range produces a **positive net-of-cost edge** clearing the **two-part panel verdict** (aggregate seven-point gate AND breadth), net of the full round-trip cost. **Falsified if** the panel verdict is KILL/INSUFFICIENT, or the §6 pre-check finds the median gross per-trade move below cost.

## 2. Economic rationale (mechanism, not pattern) — PURELY a-priori

The first `opening_range_minutes` of the session form the day's **initial balance** — the opening auction's price-discovery range. A decisive break of that range signals the day's **directional resolution** as the auction trends away from balance toward a new level. Trade the break. Purely a-priori.

## 3. Spec (event → entry → exit/holding → position)

In [`src/lab/research/strategies/opening_range_breakout.py`](../../src/lab/research/strategies/opening_range_breakout.py). `OpeningRangeBreakoutSpec` (`opening_range_breakout`), symmetric, one entry per day:

- **Event:** the opening range (running high/low over the first `opening_range_minutes`, then fixed) has **closed**.
- **Entry:** first bar with `close > OR_high·(1+break_buffer)` ⇒ LONG, `close < OR_low·(1−break_buffer)` ⇒ SHORT.
- **Exit/holding:** ride to the MIS square-off (sparse — adapter forward-fills, resets flat next open).

**Causal / no-lookahead (hard precondition).** `opening_range` is point-in-time (running, then fixed); entries fire only **after** the window closes (`timestamp ≥ session_open + window`), comparing to the fixed range. Signal at bar `i` uses only bars `≤ i`, fills at `i+1`'s open. **Prefix-invariance no-lookahead test** (`tests/unit/test_opening_range_breakout_spec.py::test_opening_range_breakout_prefix_invariance`) is a **hard precondition** — study does not run if it fails. Confirmed **PASSED**. **Distinct** from P3.2 (prior-N-bar intraday range) and P3.6 (global multi-session Donchian): the level is the OPENING range specifically.

## 4. Pre-committed parameters (FROZEN before the run, set BLIND)

| Parameter | Blind value | Sweep |
|---|---|---|
| `opening_range_minutes` | **30** | ±15 (→ 15/30/45) |
| `break_buffer` | **0.001** (0.1%) | ±0.0005 |

**5 configs** (base + two ±one-step). **Chosen BLIND, a-priori:** the classic ORB window is **30 min** (a 15-min alternative in the sweep); `break_buffer` 0.1% filters marginal pokes. **Guardrail:** nothing drawn from the continuation-gradient or `POST_PROJECT_DIRECTIONS.md`. Registry pinned by `tests/unit/test_registry.py`.

## 5. Verdict — two-part PANEL verdict (unchanged) + STRICT decisive bar

Judged by `run_panel_study` at the pre-committed thresholds, **unchanged.** **STRICT decisive bar:** less-negative is a KILL. **Prior awareness (a-priori honest):** ORB is heavily popularized and broad backtests report it "doesn't work very well anymore" — a prime overfitting/decay risk; the strict bar is exactly the guard.

## 6. Cost-viability pre-check (before the full battery)

Median `|Trade.gross_return|` on RELIANCE vs cost ≈ **0.182%**. **Landing: CLEARLY VIABLE.** RELIANCE base config → **2,488 trades / 11 yr** (0.88/day ≈ one break/day), median `|gross/trade|` **0.524% > 0.182%**. Not cost-dead. **The full battery runs (post-sign-off).**

## 7. Method-variants (ledger-charged, NOT owed)

- **Volume-filtered break** (participation confirmation); **15-min** window as a distinct primary; a re-entry-on-retest variant. None run now.

## 8. Universe & data (PANEL scope)

- **Panel (scored):** the frozen 10 (Lock A) — HDFCBANK, RELIANCE, ICICIBANK, TCS, TATASTEEL, ULTRACEMCO, TITAN, COALINDIA, DRREDDY, TATACONSUM.
- **6d held-out:** INFY, SBIN, SUNPHARMA, ADANIPORTS, NESTLEIND.
- **Interval / range:** 5-minute; 2015-02-02 → 2026-07-03, regular-session-filtered.
- **Scope stamp (auto):** large-caps only; a KILL means "no edge on large-caps".

## 9. Commitment

- Params, spec, thresholds **frozen before the run**, blind, a-priori; this commit is the proof.
- **No-lookahead precondition PASSED (hard gate).**
- **Panel verdict believed as-is** at the strict decisive bar.
- **Single verdict.** If it passes → STOP + flag + verdict-grade path.
- **Deferred, did NOT shape this prereg:** gradient / ensemble ideas are record-only, forward-only.

## 10. Run commands (post-sign-off only)

```bash
uv run python scripts/run_panel_study.py --strategy opening_range_breakout \
    --interval 5minute --start 2015-02-02 --end 2026-07-03 \
    --data-root data --ledger-dir data/ledger --config-dir config
```

Panel + held-out from the frozen `study_panel.yaml`. Exploration-grade; not written to the scorecard.

---

## Result (2026-07-10) — KILL (full panel battery) — exploration-grade

> **Exploration-grade** (frozen-49, large-cap). §0–§10 are the blind PLAN; this block is the OUTCOME. Program ledger **51 → 56** (5 configs). Not in the §4.3 scorecard.

**Cost-viability (§6):** RELIANCE median |gross/trade| **0.524%** > 0.182% → not cost-dead; the full battery ran.

**Panel verdict: KILL** — Part 1 KILL, Part 2 breadth FAIL. Breadth median per-symbol CPCV path-Sharpe **−2.406**, positive fraction **0.00**; aggregate CPCV median **−5.395**, DSR 0.000, PBO 0.143, effective trials 7.74.

| # | Criterion | Result |
|---|---|---|
| 1 | CPCV median path-Sharpe > 1.0 | **FAIL** — −5.395 |
| 2 | DSR ≥ 0.95 | **FAIL** — 0.000 |
| 3 | PBO < 0.20 | **PASS** — 0.143 (elevated, still within the bar) |
| 4 | CPCV ≥ 90% positive & p10 ≥ 0 | **FAIL** — positive 0.00, p10 −7.378 |
| 5 | PF ≥ 1.3, top-5 < 40%, net expectancy > 0 | **FAIL** — PF 0.37, top5 0.07, net expectancy −0.00150 |
| 6 | Robustness battery | **FAIL** — param −5.66, MC 0.00, cross-sym 0.00, noise False; two-engine reconciles ✓ |
| 7 | Regime stability | **FAIL** — positive in no bucket |

**Diagnosis:** the popularized opening-range breakout shows **no edge on large-caps** — aggregate −5.395, 0/10 symbols positive, PF 0.37. **PBO 0.143 is the highest of the batch** (more config-sensitivity, consistent with the "crowded/decayed, prime overfitting risk" a-priori prior) but still passes criterion 3. KILL under the strict bar.

**Disposition.** Honest logged negative; nothing tuned. Exploration-grade / large-cap; not in the §4.3 scorecard.
