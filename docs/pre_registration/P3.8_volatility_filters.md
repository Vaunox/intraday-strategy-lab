# Pre-registration — P3.8 · Volatility-based filters (two independent regime studies), PANEL scope

> **Status: SIGNED OFF (operator, 2026-07-10).** Two INDEPENDENT standalone studies (not a dichotomy), ATR-ratio regime, blind params, strict-decisive-bar — confirmed before any run. This document's git commit **predates the first test run**.
> **Exploration-grade** (frozen-49 survivor-only, large-cap panel): provisional / upper-bound + large-cap scope stamp regardless of PASS/KILL; **not** written to the `RESEARCH_FINDINGS.md §4.3` scorecard as a verdict.
> **Scored under PANEL scope** (`run_panel_study`, per-symbol-then-aggregate).

## 0. Two INDEPENDENT studies (not a both-owed dichotomy)

A volatility filter has no standalone edge to measure — it only means something applied on top of a directional signal. So P3.8 tests the volatility-filter thesis as **two self-contained, blind, regime-conditional strategies**, defined whole (regime + signal together), run as **two INDEPENDENT standalone studies** — each its own verdict, each can pass or fail on its own:

- **C1 — Expansion → Breakout** (`vol_expansion_breakout`).
- **C2 — Contraction → Mean-Reversion** (`vol_contraction_reversion`).

**They are NOT a both-owed dichotomy.** C1 and C2 are *compatible, independent* strategies operating in *disjoint* regimes; both passing would be a coherent "breakouts work in expansion AND reversion works in contraction" (the filter thesis confirmed), NOT a paradox. Forcing dichotomy / "if both pass, contradiction" logic on them would be false framing. Running **both** serves one purpose only: **it removes all selection ambiguity** — neither regime-direction is cherry-picked from prior results — and answers the symmetric question (does regime-conditioning help either family?). Either passing → STOP + flag + verdict-grade path.

## 1. Hypotheses (falsifiable) — per study

On the frozen large-cap **panel** at 5-minute bars, each study produces a **positive net-of-cost edge** clearing the **two-part panel verdict** (aggregate seven-point gate on the equal-weight portfolio AND breadth: median per-symbol CPCV path-Sharpe > 1.0 and ≥ 60% of symbols individually positive), net of the full Indian round-trip cost, realized-frequency annualized. **Falsified if** the panel verdict is KILL/INSUFFICIENT, or the cost-viability pre-check finds the median gross per-trade move below the modeled round-trip cost.

## 2. Economic rationale (mechanism, not pattern) — PURELY a-priori

Both reasons are true **before any result** — no reference to prior outcomes.

- **C1 (Expansion → Breakout):** a breakout **mechanically requires volatility to be expanding** — a trading range resolves into a directional move only when dispersion widens; a "break" in *contracting* volatility has no move to break into and reverts. So gate breakout entries to an expanding-vol regime.
- **C2 (Contraction → Mean-Reversion):** mean-reversion **requires a quiet range to oscillate within** — in *contracting* volatility there is no directional energy, so price reverts to the mean; in expanding volatility a fade fights a real move. So gate reversion entries to a contracting-vol regime.

## 3. Specs (event → entry → exit/holding → position)

Both in [`src/lab/research/strategies/volatility_filters.py`](../../src/lab/research/strategies/volatility_filters.py); both gate a directional rule by a **shared ATR-ratio regime** (`indicators.atr_ratio` = `atr(atr_short) / atr(atr_long)`, dimensionless, self-normalizing per symbol — directly comparable across the panel without a rank; causal — both ATRs from completed prior bars).

- **C1 — `VolExpansionBreakoutSpec`** (`vol_expansion_breakout`): when `atr_ratio > 1.0` (recent ATR above the longer baseline → expanding), trade the prior-N-bar intraday breakout — `close > intraday_donchian_high` ⇒ LONG, `close < low` ⇒ SHORT; ride to the opposite in-regime break or 15:20 square-off. Out of regime (`≤ 1.0`) → no new entry.
- **C2 — `VolContractionReversionSpec`** (`vol_contraction_reversion`): when `atr_ratio < 1.0` (contracting), fade the intraday z-score — `intraday_zscore > +entry_z` ⇒ SHORT, `< −entry_z` ⇒ LONG, exit when `|z| ≤ exit_z`; wide-entry/narrow-exit hysteresis. Out of regime (`≥ 1.0`) → no new entry.
- Each day opens flat; 15:20 square-off; fixed unit size.

**Causal / no-lookahead (hard precondition, both studies).** The regime, signal, and position at bar `i` use only bars `≤ i` (ATR and `intraday_zscore` are trailing/prior-only; `intraday_donchian` excludes the current bar). The shared **`atr_ratio` primitive has its own prefix-invariance test** (`talib.ATR` confirmed prefix-invariant), and **each spec has a prefix-invariance no-lookahead test** — all **hard preconditions**: if any fails, that study does not run.

## 4. Pre-committed parameters (FROZEN before the run, set BLIND)

| Study | Regime (shared) | Directional | Swept params | Fixed blind |
|---|---|---|---|---|
| C1 | `atr_ratio(20,100) > 1.0` | intraday breakout | `breakout_lookback` **20** ±5, `atr_long` **100** ±20 | `atr_short` **20** |
| C2 | `atr_ratio(20,100) < 1.0` | z-score fade | `entry_z` **2.0** ±0.5, `atr_long` **100** ±20 | `atr_short` **20**, `exit_z` **0.5**, `lookback` **20** |

**5 configs each** (base + two ±one-step params). **Chosen BLIND, a-priori:** `atr_short` 20 (~intraday, ~100 min) vs `atr_long` 100 (~1.3 sessions baseline) — recent dispersion vs a longer baseline; the ratio boundary at **1.0** ("above/below this stock's own baseline") is the natural, threshold-free split (no arbitrary cutoff). **Not** derived from any regime-performance breakdown (none seen; criterion-7 was "positive in no bucket"). The `atr_long`± sweep tests that the result is not cliff-sensitive to the boundary. **One regime condition each; no stacking.** Registry entries pinned by `tests/unit/test_registry.py`.

## 5. Verdict — two-part PANEL verdict at the standing thresholds (unchanged) + STRICT decisive bar

Judged by `run_panel_study` at the pre-committed `config/killgate.yaml` thresholds, **unchanged.** BOTH parts required per study: (1) aggregate equal-weight portfolio clears the seven criteria; (2) breadth (median per-symbol CPCV path-Sharpe > 1.0 AND ≥ 60% positive).

**STRICT decisive bar (non-negotiable).** This is the most-motivated study in the catalog, which is exactly where a marginal result gets over-read. **A regime-conditional strategy coming back "less negative" is a KILL, not a partial win.** "It improved things" is not progress; a PASS requires clearing the two-part gate **decisively**. No threshold altered.

## 6. Cost-viability pre-check (per study, before the full battery)

Median `|Trade.gross_return|` on RELIANCE vs `cost_model.round_trip_cost_fraction(100_000)` ≈ 0.18%; below cost ⇒ cost-dead (logged finding, skip the battery). Measured fresh per study.

## 7. Universe & data (PANEL scope)

- **Panel (scored):** the frozen 10 (Lock A) — HDFCBANK, RELIANCE, ICICIBANK, TCS, TATASTEEL, ULTRACEMCO, TITAN, COALINDIA, DRREDDY, TATACONSUM.
- **6d held-out (never scored):** INFY, SBIN, SUNPHARMA, ADANIPORTS, NESTLEIND.
- **Interval / range:** 5-minute; 2015-02-02 → 2026-07-03, regular-session-filtered.
- **Scope stamp (auto):** large-caps only (frozen-49, ADV ₹122–1647 cr); a KILL means "no edge on large-caps", not "no edge anywhere".

## 8. Commitment

- Params, both specs, the shared ATR-ratio regime, and thresholds are **frozen before the first run**, set **blind**, pure a-priori; this commit is the proof.
- **The `atr_ratio` prefix-invariance test AND each spec's no-lookahead test are hard preconditions** — a study does not run if its precondition fails.
- **The panel verdict is believed as-is,** at the **strict decisive bar.** No tuning to rescue a result; less-negative is a KILL.
- **Two independent verdicts.** If **either** passes → STOP + flag + verdict-grade path. Both passing is the coherent thesis-confirmed case (not a contradiction), still investigated via the verdict-grade path.
- **Deferred, documented:** the "can a filter rescue a *chosen* least-dead base?" thesis is irreducibly post-hoc (base selection cannot be de-biased inside the catalog) → deferred to the post-project ensemble phase.

## 9. Run commands (post-sign-off only)

```bash
uv run python scripts/run_panel_study.py --strategy vol_expansion_breakout \
    --interval 5minute --start 2015-02-02 --end 2026-07-03 \
    --data-root data --ledger-dir data/ledger --config-dir config
uv run python scripts/run_panel_study.py --strategy vol_contraction_reversion \
    --interval 5minute --start 2015-02-02 --end 2026-07-03 \
    --data-root data --ledger-dir data/ledger --config-dir config
```

The panel + held-out sets are read from the frozen `study_panel.yaml` (not CLI args). Exploration-grade; not written to the scorecard.

---

## Result (run 2026-07-09/10) — BOTH KILL (C1 battery, C2 battery via §6-override) — exploration-grade

> **Exploration-grade** (frozen-49, large-cap panel): provisional / upper-bound + large-cap stamp, **not** in the `RESEARCH_FINDINGS.md §4.3` scorecard. §0–§9 are the PLAN, committed **before** the runs (PR #40, all pre-run); this block is the OUTCOME. Two INDEPENDENT studies; program ledger **36 → 46** (C1's 5 + C2's 5 configs), effective 6.12.

### C1 — `vol_expansion_breakout` (breakout gated to EXPANDING vol) — FULL BATTERY

**Cost-viability (§6):** RELIANCE median |gross/trade| **0.559%** > 0.182% → not cost-dead; the full battery ran.

**Panel verdict: KILL** — Part 1 (aggregate gate) KILL, Part 2 (breadth) FAIL. Breadth median per-symbol CPCV path-Sharpe **−3.057**, positive fraction **0.00**; aggregate CPCV median **−7.360**, DSR 0.000, PBO 0.000.

| # | Criterion | Result |
|---|---|---|
| 1 | CPCV median path-Sharpe > 1.0 | **FAIL** — −7.360 |
| 2 | DSR ≥ 0.95 | **FAIL** — 0.000 |
| 3 | PBO < 0.20 | **PASS** — 0.000 |
| 4 | CPCV ≥ 90% positive & p10 ≥ 0 | **FAIL** — positive 0.00, p10 −8.552 |
| 5 | PF ≥ 1.3, top-5 < 40%, net expectancy > 0 | **FAIL** — PF 0.27, top5 0.05, net expectancy −0.00229 |
| 6 | Robustness battery | **FAIL** — param −8.08, MC 0.00, cross-sym 0.00, noise False; two-engine reconciles ✓ |
| 7 | Regime stability | **FAIL** — positive in no bucket |

**Diagnosis:** the vol-expansion gate **helped** the breakout — aggregate −7.360 vs the ungated P3.2 breakout's −8.753 (PF 0.27 vs 0.23), the §4.2 "vol filters help but rarely enough" pattern — but 0/10 positive, net expectancy −0.229% < 0, so **KILL under the strict decisive bar** (less-negative is not a win). It did NOT beat the slowest-signal continuation (Donchian −6.13), which remains the least-bad continuation bet.

### C2 — `vol_contraction_reversion` (z-fade gated to CONTRACTING vol) — FULL BATTERY (via §6 override)

**Cost-viability (§6): borderline cost-dead on RELIANCE** — 0.174% < 0.182% (a **−0.008%** margin). **§6-skip OVERRIDDEN (operator, 2026-07-10) — full panel run.**

> **Override scope (recorded so §6 stays intact):** justified **ONLY** by (a) the margin being within measurement noise (−0.008%, vs V2 slope's clear −0.060% which was §6-skipped), AND (b) the §6 pre-check being **single-symbol (RELIANCE)** while the verdict is **panel-wide**, so a one-stock read could misstate the ten-stock verdict. **NOT a general "run borderline cost-dead cases" rule** — clearly-below-cost strategies (like V2 slope) still get §6-skipped, full stop.

**Panel verdict: KILL** — Part 1 KILL, Part 2 breadth FAIL. Breadth median per-symbol CPCV path-Sharpe **−10.616**, positive fraction **0.00**; aggregate CPCV median **−25.636**, DSR 0.000, PBO 0.000.

| # | Criterion | Result |
|---|---|---|
| 1 | CPCV median path-Sharpe > 1.0 | **FAIL** — −25.636 |
| 2 | DSR ≥ 0.95 | **FAIL** — 0.000 |
| 3 | PBO < 0.20 | **PASS** — 0.000 |
| 4 | CPCV ≥ 90% positive & p10 ≥ 0 | **FAIL** — positive 0.00, p10 −28.520 |
| 5 | PF ≥ 1.3, top-5 < 40%, net expectancy > 0 | **FAIL** — PF 0.00, top5 0.32, net expectancy −0.00390 |
| 6 | Robustness battery | **FAIL** — param −32.82, MC 0.00, cross-sym 0.00, noise False; two-engine reconciles ✓ |
| 7 | Regime stability | **FAIL** — positive in no bucket |

**The override was vindicated:** the panel verdict (**−25.636, PF 0.00** — catastrophically dead, near ungated MR's −27.040) is *far more decisive* than the borderline single-symbol cost-viability (0.174%) suggested. The one-stock read badly understated the panel deadness — exactly the single-symbol-vs-panel mismatch the override guarded against. Running it gave the honest, decisive answer.

### Both independent studies — KILL. Filter thesis closed.

They are independent (not a dichotomy), so both KILLing has no cross-study logic to resolve. The volatility-filter thesis, tested symmetrically: **vol-gating helped *continuation* (breakout −8.75 → −7.36) more than *reversion* (MR −27.04 → −25.64, still PF 0.00), and rescued neither** — "help but rarely enough." No edge either way on large-caps.

### Disposition

Honest logged negatives; nothing tuned; the shared causal ATR-ratio regime + the no-lookahead proofs stand. Both exploration-grade / large-cap; **not** in the §4.3 scorecard. Cumulative program ledger after P3.8: **46 trials → effective 6.12**. Continuation-gradient updated (`PROGRESS.md`): C1 (−7.36) is the **second-least-bad** continuation bet, behind Donchian (−6.13) — the vol-gate narrowed the breakout's gap but did not overtake the low-turnover Donchian.
