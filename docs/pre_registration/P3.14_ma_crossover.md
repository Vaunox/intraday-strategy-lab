# Pre-registration — P3.14 · Moving Average Crossover (fast/slow SMA), PANEL scope

> **Status: DRAFT — committed before any run, HELD for operator sign-off.** Part of the P3.10–P3.14 blind batch (operator ruling 2026-07-10). Single trend study; blind params; strict decisive bar. This commit **predates the panel run**.
> **Exploration-grade** (frozen-49 survivor-only, large-cap panel): provisional + large-cap scope stamp; **not** in the `RESEARCH_FINDINGS.md §4.3` scorecard.
> **Scored under PANEL scope** (`run_panel_study`).

## 0. A single self-contained study

The canonical fast/slow MA crossover — one **trend/continuation** mechanism (symmetric long/short). SMA vs EMA is a **method choice** (SMA primary, EMA a non-owed ledger variant, §7), not a direction. **Distinct from P3.7** (adaptive KAMA cross): plain, non-adaptive SMAs.

## 1. Hypothesis (falsifiable)

On the frozen large-cap **panel** at 5-min bars, holding the trend side of a fast/slow SMA crossover produces a **positive net-of-cost edge** clearing the **two-part panel verdict**, net of cost. **Falsified if** the panel verdict is KILL/INSUFFICIENT, or the §6 pre-check finds the median gross per-trade move below cost.

## 2. Economic rationale (mechanism, not pattern) — PURELY a-priori

A fast moving average crossing a slow one marks a **shift in the prevailing trend**; holding the trend side (long while fast > slow) captures the **sustained move**. The canonical "great in a trend, whipsawed in chop" rule. Purely a-priori.

## 3. Spec (event → entry → exit/holding → position)

In [`src/lab/research/strategies/ma_crossover.py`](../../src/lab/research/strategies/ma_crossover.py). `MaCrossoverSpec` (`ma_crossover`), symmetric, intraday:

- **Position:** per bar, target the **sign of `SMA(fast_period) − SMA(slow_period)`** — LONG while fast > slow, SHORT while below, flat during warmup.
- **Event:** the position **flips on each crossover**; squared off at the MIS cutoff (the adapter resets flat each day).

**Causal / no-lookahead (hard precondition).** SMAs are trailing (`talib.SMA`, prefix-invariant); signal at bar `i` uses only bars `≤ i`, fills at `i+1`'s open. **Prefix-invariance no-lookahead test** (`tests/unit/test_ma_crossover_spec.py::test_ma_crossover_prefix_invariance`) is a **hard precondition** — study does not run if it fails. Confirmed **PASSED**.

## 4. Pre-committed parameters (FROZEN before the run, set BLIND)

| Parameter | Blind value | Sweep |
|---|---|---|
| `fast_period` | **20** | ±5 |
| `slow_period` | **50** | ±10 |

**5 configs** (base + two ±one-step). **Chosen BLIND, a-priori:** a classic intraday fast/slow pair **20/50** (distinct from P3.7's KAMA 10/30; the 20/50 SMA pair is textbook). **Guardrail:** `slow_period` is set from textbook reasoning, **not** from the continuation-gradient "slower = less-bad" observation (a deferred, forward-only cross-study signal in `POST_PROJECT_DIRECTIONS.md`) — biasing the window toward the gradient would leak a post-hoc result into a blind param. Registry pinned by `tests/unit/test_registry.py`.

## 5. Verdict — two-part PANEL verdict (unchanged) + STRICT decisive bar

Judged by `run_panel_study` at the pre-committed thresholds, **unchanged.** **STRICT decisive bar:** less-negative is a KILL. **Prior awareness (a-priori honest):** broad evidence of MA-crossover decay after costs; the strict bar is the guard. Expect clustering with P3.7 (KAMA cross) and the other continuation bets in effective-N.

## 6. Cost-viability pre-check (before the full battery)

Median `|Trade.gross_return|` on RELIANCE vs cost ≈ **0.182%**. **Landing: CLEARLY VIABLE.** RELIANCE base config → **7,164 trades / 11 yr** (2.54/day), median `|gross/trade|` **0.362% > 0.182%**. Not cost-dead (a slower trend flip than P3.7-V2's cost-dead 30k-trade KAMA slope). **The full battery runs (post-sign-off).**

## 7. Method-variants (ledger-charged, NOT owed)

- **EMA** instead of SMA (faster response); different fast/slow pairs; a trend-strength (ADX) filter; **15-min** frequency. None run now.

## 8. Universe & data (PANEL scope)

- **Panel (scored):** the frozen 10 (Lock A) — HDFCBANK, RELIANCE, ICICIBANK, TCS, TATASTEEL, ULTRACEMCO, TITAN, COALINDIA, DRREDDY, TATACONSUM.
- **6d held-out:** INFY, SBIN, SUNPHARMA, ADANIPORTS, NESTLEIND.
- **Interval / range:** 5-minute; 2015-02-02 → 2026-07-03, regular-session-filtered.
- **Scope stamp (auto):** large-caps only; a KILL means "no edge on large-caps".

## 9. Commitment

- Params, spec, thresholds **frozen before the run**, blind, a-priori; this commit is the proof.
- **No-lookahead precondition PASSED (hard gate).**
- **Panel verdict believed as-is** at the strict decisive bar; no tuning to rescue.
- **Single verdict.** If it passes → STOP + flag + verdict-grade path.
- **Deferred, did NOT shape this prereg:** the gradient (esp. "slower = less-bad") and ensemble ideas are record-only, forward-only — explicitly kept out of the `slow_period` choice.

## 10. Run commands (post-sign-off only)

```bash
uv run python scripts/run_panel_study.py --strategy ma_crossover \
    --interval 5minute --start 2015-02-02 --end 2026-07-03 \
    --data-root data --ledger-dir data/ledger --config-dir config
```

Panel + held-out from the frozen `study_panel.yaml`. Exploration-grade; not written to the scorecard.
