# Pre-registration — P3.10 · Gap and Go (confirmed opening-gap continuation), PANEL scope

> **Status: DRAFT — committed before any run, HELD for operator sign-off.** Part of the P3.10–P3.14 blind batch (operator ruling 2026-07-10). Single continuation study; blind params; strict decisive bar. This commit **predates the panel run** (the honesty proof).
> **Exploration-grade** (frozen-49 survivor-only, large-cap panel): provisional / upper-bound + large-cap scope stamp regardless of PASS/KILL; **not** written to the `RESEARCH_FINDINGS.md §4.3` scorecard as a verdict.
> **Scored under PANEL scope** (`run_panel_study`, per-symbol-then-aggregate).

## 0. A single self-contained study (natural-primary direction)

"Gap and Go" is the **continuation** reading of an opening gap. Its opposite — the "gap fill" fade — is the other direction; per the handoff, only P3.1 / P3.7 / P3.13 are **both-owed** dichotomies, so this is the **natural-primary** pick (the strategy is literally named for continuation). The fade is **now promoted to a both-owed twin** (operator ruling, 2026-07-10 — a directional opposite must not be direction-cherry-picked): it is drafted as **`gap_fade` (P3.10b)** and **run now** under panel scope as a genuine distinct ledger-charged trial — same qualifying gap event, trigger flipped to **VWAP rejection**, faded toward the fill. This supersedes the earlier "deferred ungated study" framing (run now, not deferred). See §7 and the [P3.10b gap_fade pre-registration](P3.10b_gap_fade.md).

## 1. Hypothesis (falsifiable)

On the frozen large-cap **panel** at 5-minute bars, entering in the direction of a **confirmed opening gap** (large gap + participation + price holding on the gap side of VWAP) produces a **positive net-of-cost edge** clearing the **two-part panel verdict** (aggregate seven-point gate AND breadth: median per-symbol CPCV path-Sharpe > 1.0 and ≥ 60% positive), net of the full Indian round-trip cost, realized-frequency annualized. **Falsified if** the panel verdict is KILL/INSUFFICIENT, or the §6 cost-viability pre-check finds the median gross per-trade move below the modeled round-trip cost.

## 2. Economic rationale (mechanism, not pattern) — PURELY a-priori

A large overnight gap reflects genuine **overnight information** (earnings, global moves, news). When the gap is **confirmed by early participation** (elevated volume — real money, not a thin print) AND price **holds on the gap side of the intraday VWAP** (buyers/sellers remain in control rather than fading the open), the repricing continues intraday as latecomers chase. Trade **with** the gap. Purely a-priori: no reference to any prior result.

## 3. Spec (event → entry → exit/holding → position)

In [`src/lab/research/strategies/gap_and_go.py`](../../src/lab/research/strategies/gap_and_go.py). `GapAndGoSpec` (`gap_and_go`), symmetric, one entry per day, intraday:

- **Event:** within the first `open_window` bars, the overnight `gap` qualifies (`|gap| ≥ gap_threshold`), `relative_volume ≥ vol_mult` (participation), and price is on the gap side of the intraday `vwap`.
- **Entry:** gap up + `close > VWAP` ⇒ LONG; gap down + `close < VWAP` ⇒ SHORT (first qualifying bar).
- **Exit/holding:** ride to the MIS square-off (sparse — the adapter forward-fills the entry to the day's end, resets flat next open). Fixed unit size.

**Causal / no-lookahead (hard precondition).** `gap` is known at the open (prior-day close + today's open), `vwap` is the intraday cumulative average, `relative_volume` is trailing — all point-in-time, so the signal at bar `i` uses only bars `≤ i` and fills at `i+1`'s open. The spec has a **prefix-invariance no-lookahead test** (`tests/unit/test_gap_and_go_spec.py::test_gap_and_go_prefix_invariance`) — a **hard precondition: the study does not run if it fails.** Confirmed **PASSED**.

## 4. Pre-committed parameters (FROZEN before the run, set BLIND)

| Parameter | Blind value | Sweep |
|---|---|---|
| `gap_threshold` (min \|overnight gap\|) | **0.010** (1.0%) | ±0.005 |
| `vol_mult` (`relative_volume` confirmation) | **1.2** | ±0.2 |
| `relvol_period` | **20** | fixed |
| `open_window` (bars) | **12** (~60 min) | fixed |

**5 configs** (base + two ±one-step params). **Chosen BLIND, a-priori:** a "large" gap ≈ **1%** for a large-cap; `vol_mult` 1.2 = modestly elevated participation; `relvol_period` 20 (~100 min trailing) as the participation baseline; `open_window` 12 keeps it an **opening** play (~first hour). **Guardrail:** nothing here is drawn from the continuation-gradient observation or `POST_PROJECT_DIRECTIONS.md`; all values are textbook a-priori. Registry pinned by `tests/unit/test_registry.py`.

## 5. Verdict — two-part PANEL verdict (unchanged) + STRICT decisive bar

Judged by `run_panel_study` at the pre-committed `config/killgate.yaml` thresholds, **unchanged.** BOTH parts required. **STRICT decisive bar:** a "less negative" result is a **KILL, not a partial win**; no threshold altered.

## 6. Cost-viability pre-check (before the full battery)

Median `|Trade.gross_return|` on RELIANCE vs `round_trip_cost_fraction(100_000)` ≈ **0.182%**.

**Landing: CLEARLY VIABLE.** RELIANCE base config → **189 trades / 11 yr** (0.07/day — economically expected sparsity: qualifying gap days are rare, ~monthly), median `|gross/trade|` **1.010% ≫ 0.182%**. Not cost-dead, and not a hollow count (189 trades power the median; the panel's 10 symbols give ~1,900). **The full battery runs (post-sign-off).**

## 7. Related studies & variants

**Owed twin — RUN NOW (both-owed pair, operator ruling 2026-07-10).** The **gap-fill fade** is the *directional opposite* of gap-and-go, **not** a method variant, so it must not be direction-cherry-picked. It is **promoted to a both-owed twin** and drafted as **`gap_fade` (P3.10b)** — same qualifying gap event, trigger flipped to **VWAP rejection**, faded toward the fill — **run now** under panel scope as a genuine distinct ledger-charged trial (not deferred, not life-gated). See the [P3.10b gap_fade pre-registration](P3.10b_gap_fade.md).

**Method-variants (ledger-charged, run only if the primary shows life).** First-bar continuation (green/red open) instead of VWAP side; opening-range confirmation; 15-min frequency. None run now.

## 8. Universe & data (PANEL scope)

- **Panel (scored):** the frozen 10 (Lock A) — HDFCBANK, RELIANCE, ICICIBANK, TCS, TATASTEEL, ULTRACEMCO, TITAN, COALINDIA, DRREDDY, TATACONSUM.
- **6d held-out (never scored):** INFY, SBIN, SUNPHARMA, ADANIPORTS, NESTLEIND.
- **Interval / range:** 5-minute; 2015-02-02 → 2026-07-03, regular-session-filtered.
- **Scope stamp (auto):** large-caps only (frozen-49); a KILL means "no edge on large-caps", not "no edge anywhere".

## 9. Commitment

- Params, spec, thresholds **frozen before the run**, set **blind**, pure a-priori; this commit is the proof.
- **No-lookahead prefix-invariance test is a hard precondition** (PASSED).
- **Panel verdict believed as-is** at the strict decisive bar; no tuning to rescue.
- **Single verdict.** If it passes → STOP + flag + verdict-grade path.
- **Deferred, did NOT shape this prereg:** the continuation-gradient / ensemble ideas (`POST_PROJECT_DIRECTIONS.md`) are record-only and forward-only.

## 10. Run commands (post-sign-off only)

```bash
uv run python scripts/run_panel_study.py --strategy gap_and_go \
    --interval 5minute --start 2015-02-02 --end 2026-07-03 \
    --data-root data --ledger-dir data/ledger --config-dir config
```

Panel + held-out sets from the frozen `study_panel.yaml`. Exploration-grade; not written to the scorecard.

---

## Result (2026-07-10) — KILL (full panel battery) — exploration-grade

> **Exploration-grade** (frozen-49, large-cap). §0–§10 are the blind PLAN; this block is the OUTCOME. Program ledger **46 → 51** (5 configs). Not in the `RESEARCH_FINDINGS.md §4.3` scorecard.

**Cost-viability (§6):** RELIANCE median |gross/trade| **1.010%** > 0.182% → not cost-dead; the full battery ran.

**Panel verdict: KILL** — Part 1 (aggregate gate) KILL, Part 2 (breadth) FAIL. Breadth median per-symbol CPCV path-Sharpe **−0.444**, positive fraction **0.20**; aggregate CPCV median **−0.743**, DSR 0.000, PBO 0.000, effective trials 7.41.

| # | Criterion | Result |
|---|---|---|
| 1 | CPCV median path-Sharpe > 1.0 | **FAIL** — −0.743 |
| 2 | DSR ≥ 0.95 | **FAIL** — 0.000 |
| 3 | PBO < 0.20 | **PASS** — 0.000 |
| 4 | CPCV ≥ 90% positive & p10 ≥ 0 | **FAIL** — positive 0.00, p10 −0.857 |
| 5 | PF ≥ 1.3, top-5 < 40%, net expectancy > 0 | **FAIL** — PF 0.75, top5 0.10, net expectancy −0.00038 |
| 6 | Robustness battery | **FAIL** — param −1.55, MC 0.01, cross-sym 0.00, noise False; two-engine reconciles ✓ |
| 7 | Regime stability | **FAIL** — positive in no bucket |

**Diagnosis:** the **least-bad of the run-batch** (aggregate −0.743, PF 0.75, and breadth **0.20 positive — the highest positive-fraction of any panel study to date**) but **decisively KILL** under the strict bar: negative Sharpe, PF < 1, net expectancy −0.038% < 0, only 20% of symbols positive (« 60%). A confirmed opening gap holding VWAP has a real per-trade move (1.01% gross) but no net-of-cost, risk-adjusted, broad edge on large-caps. (Sparse portfolio stream — 1,197 active days — but graded cleanly, **not** INSUFFICIENT.)

**Disposition.** Honest logged negative; nothing tuned. Exploration-grade / large-cap; not in the §4.3 scorecard. Owed twin `gap_fade` (P3.10b) is scored separately.
