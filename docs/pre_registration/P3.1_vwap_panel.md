# Pre-registration — P3.1 · VWAP (BOTH variants), PANEL scope — corrected pipeline

> **Status: DRAFT — HELD for operator sign-off (integrity gate). No study has run. DO NOT run until signed off.**
> This is the **corrected** P3.1 — the FIRST real study of the corrected pipeline, replacing the superseded single-symbol pilot (`P3.1_vwap.md`, PILOT/SUPERSEDED). Scored under **PANEL scope** (`run_panel_study`, per-symbol-then-aggregate) and the **realized-frequency** Sharpe convention.
> **Exploration-grade** (frozen-49 large-cap panel): provisional + large-cap scope stamp regardless of PASS/KILL; **not** written to the `RESEARCH_FINDINGS.md §4.3` scorecard as a verdict.
> This document's git commit **predates the first run** — the plan exists before the result.

## 0. Both variants owed (directional dichotomy)

VWAP is a genuine **directional dichotomy** (`MASTER_BLUEPRINT.md:320`, "cross / reversion"). Per the dual-variant rule, **both directions are owed** and are pre-registered here as two distinct falsifiable hypotheses, each judged once by the same unchanged panel gate:

- **V1 — Reversion (fade to VWAP):** price stretched from VWAP **reverts**.
- **V2 — Cross (trend on a decisive cross):** price that **decisively crosses** VWAP **continues** in the cross direction.

**Stated ahead of the result:** V1 and V2 are near-opposite bets on the same phenomenon (behaviour around VWAP). If **both** clear the gate that is a **contradiction to investigate, not two wins** — the same phenomenon cannot robustly both revert and continue net-of-cost. The pilot's V1 (single-symbol, pre-fix) KILLed on direction (win rate 0.48; price continued from stretched levels) — **mild prior interest in V2, not licence to expect it to pass.** Params below are **blind**; the gate rules.

**V1 also doubles as the rebuild regression check:** a known-KILL under the corrected pipeline confirms the fixed machinery end-to-end. A materially different V1 verdict must be investigated before any new result is trusted.

## 1. Hypothesis (falsifiable) — per variant

On the frozen large-cap **panel** at 5-minute bars, each variant produces a **positive net-of-cost edge** clearing the **two-part panel verdict** (aggregate seven-point gate on the equal-weight portfolio AND breadth: median per-symbol CPCV path-Sharpe > 1.0 and ≥ 60% of symbols individually positive), net of the full Indian round-trip cost, realized-frequency annualized.

**Falsified if** the panel verdict is KILL/INSUFFICIENT, or the cost-viability pre-check finds the median gross per-trade move below the modeled round-trip cost.

## 2. Economic rationale (mechanism, not pattern)

VWAP is the intraday **execution benchmark** institutions size and grade fills against; VWAP-target algorithms supply mean-reverting liquidity (buy below, sell above their running benchmark).
- **V1 (reversion):** that benchmark-driven liquidity should make **extreme deviations from VWAP partially revert**. The open question is whether the reversion beats the ~0.12–0.20% round-trip cost.
- **V2 (cross):** a **decisive** cross of VWAP on real participation marks a shift in the session's fair-value consensus — resting benchmark liquidity on the far side is absorbed — so price **continues** past VWAP. The crux is distinguishing a decisive cross from whipsaw *at* VWAP (the `cross_threshold` band + holding to square-off).

## 3. Specs (event → entry → exit/holding → position)

- **V1 — `VwapMeanReversionSpec`** ([`src/lab/research/strategies/vwap.py`](../../src/lab/research/strategies/vwap.py), registered `vwap_mean_reversion`): VWAP deviation `d = close/VWAP − 1`; from flat, `d > +entry` ⇒ SHORT, `d < −entry` ⇒ LONG; hold until `|d| ≤ exit` (wide-entry / narrow-exit hysteresis), or 15:20 square-off. Point-in-time (intraday-cumulative VWAP, daily reset). **Already implemented + registered.**
- **V2 — `VwapCrossSpec`** ([`src/lab/research/strategies/vwap.py`](../../src/lab/research/strategies/vwap.py), registered `vwap_cross` — the owed cross variant): from flat, a **decisive upside cross** (`d` rises from `≤ 0` to `> +cross_threshold`) ⇒ **LONG**; a decisive downside cross (`d` falls from `≥ 0` to `< −cross_threshold`) ⇒ **SHORT**; **ride the continuation** until the opposite decisive cross flips it or 15:20 square-off. Each day opens flat; point-in-time (same VWAP; a signal at `t` fills at `t+1` open). The `cross_threshold` band suppresses whipsaw at VWAP. Registered as `vwap_cross` in `lab.research.strategies.registry` with the blind params below (pinned by `tests/unit/test_registry.py`).

## 4. Pre-committed parameters (FROZEN, set BLIND)

**V1 reversion** (unchanged from the pilot — re-used blind, the regression anchor):

| Parameter | Value | ± one-step | Meaning |
|---|---|---|---|
| `entry_threshold` | **0.004** | ±0.001 | deviation beyond which a fade is entered |
| `exit_threshold` | **0.001** | ±0.0005 | deviation within which the fade is exited |

**V2 cross** (set BLIND, no data peeking — the decisive-cross band at the same order as the reversion scale):

| Parameter | Value | ± one-step | Meaning |
|---|---|---|---|
| `cross_threshold` | **0.002** | ±0.001 | deviation past VWAP that counts a cross as decisive (whipsaw guard) |

Interval **5-minute** (the natural frequency; the 15-min variant is a ledger-charged option only if 5-min shows life). Position size **1.0** (no sizing). Each variant sweeps base + ±one-step per tunable param for criterion-6a / PBO; **every variant charged to the ledger** (K aggregate-portfolio streams per variant).

**Why 5-minute is the natural primary for the cross variant (V2) — a cross-specific choice, not inherited from V1's fade.** V2 is judged as V1's *near-opposite bet on the same phenomenon* (behaviour around VWAP), and §0 pre-commits that if **both** variants clear the gate that is a *contradiction to investigate*. That contradiction test is clean **only if V1 and V2 run at the same decision frequency on the same substrate** — otherwise a both-pass could be a frequency artifact rather than a genuine same-phenomenon contradiction. V1 ran at 5-minute, so V2's primary is 5-minute. The competing pull is stated honestly: a continuation/cross strategy eats whipsaw and turnover, which a slower **15-minute** bar reduces and *could flip on cost* — a standalone trend study might therefore prefer 15-minute. That is exactly **why 15-minute is reserved as the ledger-charged follow-up if (and only if) 5-minute shows life**, not promoted to primary: running 5-minute first tests the *harder, higher-turnover* cost case, so a surviving edge is real rather than a gentler-frequency artifact, and the matched-pair comparison with V1 stays intact. (Frequency is pick-the-natural-primary, not both-owed — `MASTER_BLUEPRINT.md:179`, handoff §4.)

## 5. Verdict — two-part PANEL verdict at the standing thresholds (unchanged)

`run_panel_study` at the pre-committed `config/killgate.yaml` thresholds (`panel:` block + the seven criteria), **unchanged for this study**. BOTH parts required: (1) aggregate equal-weight portfolio clears the seven criteria (realized-frequency Sharpe; criterion 7 on the exogenous panel-index regime; 6d on the held-out 5); (2) breadth (median per-symbol CPCV path-Sharpe > 1.0 AND ≥ 60% positive). **No threshold altered.**

## 6. Cost-viability pre-check (per variant, before the full battery)

Median `|Trade.gross_return|` on the primary panel name (RELIANCE) vs `cost_model.round_trip_cost_fraction(100_000)` ≈ 0.18%; below cost ⇒ cost-dead (logged finding, skip the battery). V1 pilot measured 0.336% > 0.182% (not cost-dead); V2 measured fresh.

## 7. Universe & data (PANEL scope)

- **Panel (scored):** the frozen 10 (Lock A, `study_panel.yaml`) — HDFCBANK, RELIANCE, ICICIBANK, TCS, TATASTEEL, ULTRACEMCO, TITAN, COALINDIA, DRREDDY, TATACONSUM.
- **6d held-out (never scored):** INFY, SBIN, SUNPHARMA, ADANIPORTS, NESTLEIND.
- **Interval / range:** 5-minute; 2015-02-02 → 2026-07-03, regular-session-filtered.
- **Scope stamp (auto):** large-caps only (frozen-49, ADV ₹122–1647 cr); a KILL means "no edge on large-caps", not "no edge anywhere". Promising ⇒ backfill-the-5-then-rerun for a verdict — never "record the pass".

## 8. Commitment

- Params, specs, thresholds, and frozen panel/held-out are **frozen before the first run**, set **blind**; this commit is the proof.
- **The panel verdict is believed as-is.** No tuning of anything to rescue a result. Honest KILL/INSUFFICIENT is a complete finding.
- **Both variants are owed and both will be run** (V1 first, as the regression anchor); if both were to pass, that is a contradiction to investigate.

## 9. Run command (post-sign-off only — DO NOT run before sign-off)

```bash
# V1 reversion (already registered):
uv run python scripts/run_panel_study.py --strategy vwap_mean_reversion \
    --interval 5minute --start 2015-02-02 --end 2026-07-03 \
    --data-root data --ledger-dir data/ledger --config-dir config
# V2 cross (after implementing + registering vwap_cross):
uv run python scripts/run_panel_study.py --strategy vwap_cross \
    --interval 5minute --start 2015-02-02 --end 2026-07-03 \
    --data-root data --ledger-dir data/ledger --config-dir config
```

Panel + held-out from the frozen `study_panel.yaml`. Exploration-grade; not written to the scorecard.

---

## Result (run 2026-07-09) — BOTH variants KILL (corrected pipeline, panel scope, exploration-grade)

> **Exploration-grade** (frozen-49 survivor-only, large-cap panel): provisional / upper-bound + large-cap scope stamp, **not** written to the `RESEARCH_FINDINGS.md §4.3` scorecard. §0–§9 above are the PLAN, committed **before** the runs — the corrected prereg was signed off on PR #22, the §4 cross-specific frequency rationale committed `e657f2f`, and the V2 spec built `9729891`, each before its run; this block is the OUTCOME. Program ledger started Phase 3 at 0; after P3.1 it holds **8 trials → effective 1.37**. Both variants scored the frozen 10-panel (2817 portfolio days) + 5 held-out; realized-frequency Sharpe; criterion 7 on the exogenous panel-index regime.

**V1 — `vwap_mean_reversion` (fade) — the rebuild regression check.** Run first as the known-KILL anchor; it reproduced the superseded pilot (per-symbol breadth −7.018 ≈ the pilot's RELIANCE −7.095), confirming the corrected machinery end-to-end. Cost-viability was the pilot's 0.336% > 0.182% (not cost-dead).

**Panel verdict: KILL** — Part 1 (aggregate gate) KILL, Part 2 (breadth) FAIL. Breadth median per-symbol CPCV path-Sharpe **−7.018**, positive fraction **0.00**; aggregate CPCV median **−18.793**, DSR 0.000, PBO 0.000, effective trials 1.09 (at V1's run, over its 5 configs).

| # | Criterion | Result |
|---|---|---|
| 1 | CPCV median path-Sharpe > 1.0 | **FAIL** — −18.793 |
| 2 | DSR ≥ 0.95 | **FAIL** — 0.000 |
| 3 | PBO < 0.20 | **PASS** — 0.000 |
| 4 | CPCV ≥ 90% positive & p10 ≥ 0 | **FAIL** — positive 0.00, p10 −19.391 |
| 5 | PF ≥ 1.3, top-5 < 40%, net expectancy > 0 | **FAIL** — PF 0.01, top5 0.23, net expectancy −0.00505 |
| 6 | Robustness battery | **FAIL** — param −23.30, MC 0.00, cross-sym 0.00, noise False; two-engine reconciles ✓ |
| 7 | Regime stability | **FAIL** — positive in no bucket |

**V2 — `vwap_cross` (trend / continuation) — the owed cross variant.** Built + registered post-sign-off (`VwapCrossSpec`, blind `cross_threshold` 0.002, 5-min primary per the §4 rationale), proven at the call site, then run. Cost-viability (§6, measured fresh): RELIANCE median |gross/trade| **0.455%** > 0.182% round-trip → not cost-dead; the full battery ran.

**Panel verdict: KILL** — Part 1 (aggregate gate) KILL, Part 2 (breadth) FAIL. Breadth median per-symbol CPCV path-Sharpe **−4.712**, positive fraction **0.00**; aggregate CPCV median **−11.121**, DSR 0.000, PBO 0.000, effective trials 1.37 (V1+V2 clustered).

| # | Criterion | Result |
|---|---|---|
| 1 | CPCV median path-Sharpe > 1.0 | **FAIL** — −11.121 |
| 2 | DSR ≥ 0.95 | **FAIL** — 0.000 |
| 3 | PBO < 0.20 | **PASS** — 0.000 |
| 4 | CPCV ≥ 90% positive & p10 ≥ 0 | **FAIL** — positive 0.00, p10 −12.628 |
| 5 | PF ≥ 1.3, top-5 < 40%, net expectancy > 0 | **FAIL** — PF 0.17, top5 0.07, net expectancy −0.00411 |
| 6 | Robustness battery | **FAIL** — param −16.26, MC 0.00, cross-sym 0.00, noise False; two-engine reconciles ✓ |
| 7 | Regime stability | **FAIL** — positive in no bucket |

**Diagnosis (one line).** VWAP deviation carries **no robust, cost-surviving directional edge in either direction** on the large-cap panel — the fade loses on reversion (price does not reliably revert from stretch), the cross loses on continuation (false crosses + turnover; PF 0.17, net expectancy −0.411%/trade). Both KILL with **0/10** symbols individually positive.

**Not a contradiction.** V1 and V2 are near-opposite bets on the same phenomenon; **both dying is coherent** — the pre-registered contradiction case (§0: BOTH *passing*) did not occur. The pilot's "price continues from stretched levels" was a weak, noisy tendency, not an exploitable continuation edge.

**Disposition.** Two honest logged negatives; nothing tuned; the pre-registration stands (blind params frozen). Both carry the exploration-grade / large-cap stamp; **neither enters the `RESEARCH_FINDINGS.md §4.3` scorecard**. A future verdict-grade look requires backfilling the 5 current NIFTY-50 members first, then re-running — never "record the pass." Cumulative program ledger after P3.1: **8 trials → effective 1.37** (the cluster-adjustment collapsing 8 correlated one-idea trials to ~1.37 independent bets, so P3.2+ are not over-deflated).
